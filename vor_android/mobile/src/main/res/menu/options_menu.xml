<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:compat="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:applicationContext=".HereAndNowActivity">

    <item
        android:id="@+id/action_settings"
        android:title="@string/settings_title"
        android:orderInCategory="15"
        compat:showAsAction="never"
        />

    <item
        android:id="@+id/action_close_service"
        android:title="@string/close_service_title"
        android:orderInCategory="16"
        compat:showAsAction="never"
        />

    <item
        android:id="@+id/action_search"
        android:title="@string/search_title"
        android:icon="@drawable/ic_action_search"
        compat:iconifiedByDefault="false"
        compat:showAsAction="ifRoom|collapseActionView"
        compat:actionViewClass="android.support.v7.widget.SearchView"
        />

</menu>
